<!-- GENERATED by scripts/render_spec_docs.py from docs/specs/readers.read-occupancy.yaml (bridge 1e1392576edf). Do not edit: fix the spec in the bridge, sync it, re-render. -->

> Read the PTM site occupancy of a MetaMorpheus AllQuantifiedProteinGroups.tsv as one row per group, sample group, basis and modified site.

Wire verb `readers read-occupancy` · Python `pymzlib.readers.read_occupancy` (not yet shipped)

### Wraps

- [`ModificationOccupancyCell.Parse`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Omics/BioPolymerGroup/ModificationOccupancyCell.cs) in `mzLib/Omics/BioPolymerGroup/ModificationOccupancyCell.cs` at mzLib `23c2490e`
- [`SampleGroupMeasurement.CountOccupancy`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/InternalResults/IndividualResultRecords/ProteinGroupFromTsv.cs) in `mzLib/Readers/InternalResults/IndividualResultRecords/ProteinGroupFromTsv.cs` at mzLib `23c2490e`

### Parameters

| Parameter | Type | Default | Unit | Range | Meaning |
|---|---|---|---|---|---|
| `path` | `path` | required | — | — | A MetaMorpheus AllQuantifiedProteinGroups.tsv. |
| `offset` | `int` | `0` | groups | `>= 0` | Skip this many protein groups (a group with no modified sites gives no rows). A window, not a cursor: mzLib parses the whole file on every call, so paging re-reads it. |
| `limit` | `int` | absent | groups | `>= 0` | Return at most this many protein groups (a group with no modified sites gives no rows). Absent means no limit; there is deliberately no default cap. |
| `out` | `path` | absent | — | — | Write the table to this path as tab-separated text instead of returning columns. Must differ from path. Parent directories are created. |

### Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `path` | `string` | — | never | Absolute path of the input. |
| `file_type` | `string` | — | never | The mzLib SupportedFileType that was dispatched. |
| `reader` | `string` | — | never | The mzLib reader class that parsed the file. |
| `sample_labels` | `string[]` | — | never | The file's sample-group labels in header order. |
| `truncated_cell_count` | `int` | cells | never | Occupancy cells the writer cut short or replaced with 'Output too long for Excel', including those left with no complete site (which give no rows). |
| `record_count` | `int` | groups | never | Groups in the whole file, before the window. |
| `rows_not_read` | `int` | rows | yes: not counted for this table | Data rows that did not become records. |
| `retention_time_unit` | `string` | — | yes: the table has no time column | Always null for this table. |
| `caveats` | `string[]` | — | never | What this view cannot be trusted to mean for THIS file, each citing the mzLib source. |
| `column_names` | `string[]` | — | never | Column order. |
| `absent_fields` | `string[]` | — | never | Columns the view defines that this file's format has no column or source for; null in every row (BULK.md section 4). Empty when every column has a source. |
| `failed_fields` | `string[]` | — | never | 'field: ExceptionType' for each column whose read threw on some returned rows; those cells are null. |
| `excluded_fields` | `object[]` | — | never | {field, type, reason, verb} for each field with no column shape; verb names the command that carries it, or null. |
| `error` | `object` | — | yes: always, for a single path: a file that cannot be read fails the call instead | {kind, type, message}; non-null only in a files[] entry under on-error skip. |
| `returned_count` | `int` | groups | never | Groups returned in columns (the unit offset and limit count in); 0 when written to out. |
| `row_count` | `int` | rows | never | Rows in columns: more than returned_count in a long table, where one record gives several rows; 0 when written to out. |
| `offset` | `int` | groups | never | The offset applied, in groups. |
| `truncated` | `bool` | — | never | True whenever records were left out by offset or limit. |
| `columns` | `table` | — | yes: the table was written to out instead | Column name to per-row values, in column_names order. |
| `output` | `object` | — | yes: out was not given | {path, format: "tsv", row_count} when out was given. |

Per-row fields (one value per record, in `columns` or each list entry):

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `source_index` | `int` | — | never | 0-based position of the row's input in the stdin list. *Present only with `--paths-stdin`.* |
| `source_path` | `string` | — | never | Absolute path of the row's input. *Present only with `--paths-stdin`.* |
| `protein_group_name` | `string` | — | never | The group, as in read-protein-groups. |
| `sample_label` | `string` | — | never | The sample group whose cell this site came from. |
| `basis` | `string` | — | never | count (CountOccupancy_ cell, from PSM counts) or intensity (IntensityOccupancy_ cell). |
| `entity_index` | `int` | — | never | 0-based index among the cell's entities THAT HAVE SITES; not zippable with the group's accessions. |
| `position` | `int` | — | never | Position as written: 0 is the N-terminus, residues count from 1, length + 1 is the C-terminus (OccupancySite.Position). |
| `is_n_terminus` | `bool` | — | never | position == 0. |
| `modification` | `string` | — | never | ModificationIdWithMotif, e.g. 'Oxidation on M'; may contain commas and brackets. |
| `fraction` | `float` | fraction (0 to 1) | never | The fraction as printed: two decimals for count cells, four for intensity cells. |
| `numerator` | `float` | PSMs (basis count) or intensity (basis intensity) | never | Modified count, or modified intensity to four significant digits. |
| `denominator` | `float` | PSMs (basis count) or intensity (basis intensity) | never | Total count, or total intensity to four significant digits. |
| `cell_is_truncated` | `bool` | — | never | The cell this site came from was cut short; its complete sites are still rows. |

### Errors

| Kind | pyMzLib raises | When |
|---|---|---|
| `usage` | [`UsageError`][pymzlib.UsageError] | path missing or blank; file or directory not found; mzLib does not recognise the file type (the message points at readers formats) |
| `usage` | [`UsageError`][pymzlib.UsageError] | offset, limit or out given with no value; offset or limit < 0; non-integer value; out resolves to the same file as path |
| `usage` | [`UsageError`][pymzlib.UsageError] | the file is not a MetaMorpheus protein-group table |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | mzLib cannot parse the table (MzLibException). A single malformed cell does NOT fail the read: it is named in failed_fields and gives no rows |
| `usage` | [`UsageError`][pymzlib.UsageError] | paths-stdin with path, offset or limit; paths-stdin with a value; no paths on stdin; a repeated path; threads 0 or below -1; on-error not fail or skip; on-error skip with a single path; out naming an input |
| `usage` | [`UsageError`][pymzlib.UsageError] | with on-error fail, an input that is missing or not this view: the usage error of that input, prefixed 'Input <i> (<path>)' |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | with on-error fail, mzLib failing to parse an input: that input's own error type, message prefixed 'Input <i> (<path>)' |
| `service_unavailable` | [`ServiceUnavailableError`][pymzlib.ServiceUnavailableError] | never raised by this verb |

### Caveats

- Trust numerator/denominator on basis count rows and fraction on basis intensity rows: each cell rounds the other (ModificationOccupancyCell).
- entity_index cannot be zipped with protein_group_name's accessions: an entity with no sites is dropped from the cell.
- A malformed cell is refused by mzLib (FormatException), named in failed_fields as count_occupancy or intensity_occupancy, and gives no rows.
- A truncated cell keeps its complete sites with cell_is_truncated true; truncated_cell_count counts every cut cell, including those with no rows.
- offset and limit count GROUPS; a group with no modified sites gives no rows.
- Many files: --paths-stdin reads the list in ONE process, --threads at a time, into one long table with source_index and source_path first. The output does not depend on --threads (a tested property); the default of 1 is a memory choice, since every reader materialises the whole file.

### Example

```python
result = pymzlib.readers.read_occupancy('MetaMorpheus_1.1.11_AllQuantifiedProteinGroups.tsv')
```

Recorded from the live bridge as [`readers_occupancy.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_occupancy.json). The doctests in the API reference replay these recordings in CI.

### Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.readers.read_occupancy` | `mzlib::readers::read_occupancy_with` with `ReadOptions` | `readers_read_occupancy` |

### References

None: no publication is attached to this verb.

### Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | not yet shipped | not yet shipped |

!!! warning "Not yet verified"

    - The _many spellings (py, rust, r) follow the 2026-09-23 cross-binding decision; Rust and R names here are intended, not yet ported, and since.pymzlib stays null until release.
    - Peptide-level occupancy: ModificationOccupancyCell also parses peptide tables' cells, but QuantifiedPeptideFromTsv at the pin exposes none, so this verb reads protein groups only.
    - numerator and denominator are floats for both bases; a count-basis value crosses as a whole number (1, not 1.0) and bindings must read it as a float.
