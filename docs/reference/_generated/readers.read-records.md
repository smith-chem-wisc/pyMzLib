<!-- GENERATED by scripts/render_spec_docs.py from docs/specs/readers.read-records.yaml (bridge 0388518f9f4f). Do not edit: fix the spec in the bridge, sync it, re-render. -->

> Read any file mzLib recognises into a table of that format's own record fields, naming every field that could not become a column.

Wire verb `readers read-records` · Python [`pymzlib.readers.read_records`][pymzlib.readers.read_records]

### Wraps

- [`FileReader.ReadResultFile`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/FileReader.cs) in `mzLib/Readers/FileReader.cs` at mzLib `23c2490e`
- [`IResultFile.LoadResults`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/BaseClasses/IResultFile.cs) in `mzLib/Readers/BaseClasses/IResultFile.cs` at mzLib `23c2490e`

### Parameters

| Parameter | Type | Default | Unit | Range | Meaning |
|---|---|---|---|---|---|
| `path` | `path` | required | — | — | Any of the file types readers formats lists (36 at mzLib 1.0.592), including spectra files and the Bruker .d directories. |
| `offset` | `int` | `0` | records | `>= 0` | Skip this many records. A window, not a cursor: mzLib parses the whole file on every call, so paging re-reads it. For a large file use out in one call. |
| `limit` | `int` | absent | records | `>= 0` | Return at most this many records. Absent means no limit; there is deliberately no default cap. |
| `out` | `path` | absent | — | — | Write the selected window as a tab-separated table here instead of returning columns (header = column_names). Must differ from path. Parent directories are created. |

### Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `path` | `string` | — | never | Absolute path of the input. |
| `file_type` | `string` | — | never | The SupportedFileType mzLib dispatched, not a guess from the extension. |
| `reader` | `string` | — | never | The mzLib reader class that parsed it. |
| `record_type` | `string` | — | never | The mzLib record class the columns are properties of (e.g. ToppicPrsm, MzIdentMLRecord, ProteinGroupFromTsv), so a column is cross-referenceable against the mzLib source. |
| `views` | `string[]` | — | never | The cross-format views this file also offers; same values as readers formats. |
| `record_count` | `int` | records | never | Records mzLib parsed from the whole file, before the window. Records, not lines: see caveats. |
| `returned_count` | `int` | records | never | Records in columns; 0 when written to out. |
| `offset` | `int` | records | never | The offset applied. |
| `truncated` | `bool` | — | never | True whenever records were left out by offset or limit. |
| `excluded_fields` | `table` | — | never | Every property of record_type that could not become a column, as {field, type, reason}: nested objects, lists of composites, and dictionaries (reason starts "a dictionary"). Empty when every property projects. |
| `failed_fields` | `string[]` | — | never | "field: ExceptionType" for every property that threw while being read on a returned record; those cells are null. |
| `column_names` | `string[]` | — | never | The record type's projectable public properties in snake_case, base class first, in declaration order. |
| `columns` | `table` | — | yes: the table was written to out instead | Column name to per-record values, in column_names order. |
| `output` | `object` | — | yes: out was not given | {path, format: "tsv", row_count} when out was given; row_count is the window written, not the whole file. |

**The columns are per-format**: they are the file's own record fields, listed in `column_names` on every result. Every cell follows these rules:

- A cell is null when mzLib's value is null, when a double is NaN or infinite (JSON cannot carry it), or when the property threw (then named in failed_fields).
- -1 crosses unchanged: in a format's own columns it is often a real measurement. The one exception is retention_time and monoisotopic_mass on IQuantifiableRecord types (psmtsv, osmtsv, MsFraggerPsm, DiaNnReport), where mzLib documents -1 as absent; those are null, matching read-results.
- Units are the writing tool's and are not normalised: retention time is minutes for psmtsv/osmtsv/MsFraggerPsm/DiaNnReport and undeclared for the rest. read-records carries no retention_time_unit; use a typed view when units matter.
- Enums cross as their member names, dates as ISO 8601 strings, and a list of scalars as ONE ';'-joined string, on the wire and in out alike; split it yourself.

### Errors

| Kind | pyMzLib raises | When |
|---|---|---|
| `usage` | [`UsageError`][pymzlib.UsageError] | path missing or blank; file or directory not found |
| `usage` | [`UsageError`][pymzlib.UsageError] | mzLib does not recognise the file type (the message points at readers formats) |
| `usage` | [`UsageError`][pymzlib.UsageError] | offset, limit or out given with no value; offset or limit < 0; non-integer value; out resolves to the same file as path |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | the file is recognised but mzLib fails to parse it; the error type is mzLib's own (e.g. HeaderValidationException, MzLibException) |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | a vendor format that cannot load on this platform surfaces as its real failure (Bruker/timsTOF native DLLs are Windows x64 only) |
| `service_unavailable` | [`ServiceUnavailableError`][pymzlib.ServiceUnavailableError] | never raised by this verb |

### Caveats

- Columns are this format's own fields under mzLib's names. Two formats do not share columns, and a same-named column need not mean the same thing; use a typed view (read-results, read-features, read-matches, read-spectra) for comparable columns.
- No field is silently dropped: anything that cannot be a column is in excluded_fields. At mzLib 1.0.592 that includes the most important values of three new readers, which are dictionaries: MetaMorpheusQuantifiedProteinGroups.sample_groups (per-sample intensity, spectral count, occupancy), FlashLFQQuantifiedPeptide.samples (per-run intensity, detection type, RT) and MzIdentML/MzIdentMLGz.scores (engine scores).
- record_count counts what mzLib parsed, not lines. Rows mzLib drops are not reported here: the psmtsv readers discard malformed lines (read-results reports rows_not_read; this verb does not), and the mzIdentML reader skips items it cannot represent into SkippedMatches, which the bridge does not report.
- Since mzLib 1.0.592 (#1346), pro_forma on psmtsv/osmtsv from MetaMorpheus 1.1.11 or earlier is computed from full_sequence instead of null; ambiguous or unconvertible rows stay null. Measured cost: 12.9 s -> 15.6 s on a 271,551-row psmtsv.
- Since mzLib 1.0.592 (#1345), mbr_score on FlashLFQQuantifiedPeak is null, not 0, when a peak has no score, and current-format peaks tables (no MBR Score column) read, with seven more columns.
- offset/limit window after a full parse; memory and time scale with the file, not the window.

### Example

```python
result = pymzlib.readers.read_records(path, limit=3)
```

Recorded from the live bridge as [`readers_records_crux.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_records_crux.json). The doctests in the API reference replay these recordings in CI.

```python
result = pymzlib.readers.read_records(path)
```

Recorded from the live bridge as [`readers_records_toppic.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_records_toppic.json). The doctests in the API reference replay these recordings in CI.

```python
result = pymzlib.readers.read_records(path, limit=3)
```

Recorded from the live bridge as [`readers_records_mzid_gz.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_records_mzid_gz.json). The doctests in the API reference replay these recordings in CI.

```python
result = pymzlib.readers.read_records(path, limit=2)
```

Recorded from the live bridge as [`readers_records_mm_protein_groups.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_records_mm_protein_groups.json). The doctests in the API reference replay these recordings in CI.

```python
result = pymzlib.readers.read_records(path)
```

Recorded from the live bridge as [`readers_records_mm_peptides.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_records_mm_peptides.json). The doctests in the API reference replay these recordings in CI.

### Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.readers.read_records` | `mzlib::readers::read_records_with` with `ReadOptions` | `readers_read_records` |

### References

None: no publication is attached to this verb.

### Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | 0.1.0 | 0.1.0 | 0.1.0 |

!!! warning "Not yet verified"

    - The three mzLib 1.0.592 fixtures (mzid_gz, mm_protein_groups, mm_peptides) exist only on pyMzLib#61 until it merges; mzLibRust and mzLibR have not copied them.
