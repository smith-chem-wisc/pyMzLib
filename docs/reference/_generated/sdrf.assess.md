<!-- GENERATED by scripts/render_spec_docs.py from docs/specs/sdrf.assess.yaml (bridge 0388518f9f4f). Do not edit: fix the spec in the bridge, sync it, re-render. -->

> Decide whether each SDRF file's sample half describes an experimental design - Informative, Partial or Skeleton - with the per-column counts behind the verdict.

Wire verb `sdrf assess` · Python `pymzlib.sdrf.assess` (not yet shipped)

### Wraps

- [`SdrfSampleInformativeness.Assess`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/Sdrf/SdrfSampleInformativeness.cs) in `mzLib/Readers/Sdrf/SdrfSampleInformativeness.cs` at mzLib `23c2490e`
- [`SdrfColumnCoverage`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/Sdrf/SdrfCoverage.cs) in `mzLib/Readers/Sdrf/SdrfCoverage.cs` at mzLib `23c2490e`

### Parameters

| Parameter | Type | Default | Unit | Range | Meaning |
|---|---|---|---|---|---|
| `path` | `path` | absent | — | — | One .sdrf.tsv file. Exactly one of path and paths-stdin. |
| `paths_stdin` (wire `--paths-stdin`) | `flag` | absent | — | — | Many paths on stdin, as for sdrf validate. |
| `threads` | `int` | `1` | documents | `>= 1, or -1` | Documents assessed at once; output identical at any value. |
| `on_error` (wire `--on-error`) | `string` | `fail` | — | `fail \| skip` | As for sdrf validate. |

### Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `path` | `string` | — | never | The absolute path assessed. |
| `verdict` | `string` | — | never | Informative (all three checks pass), Skeleton (none), Partial (between). mzLib's SdrfSampleVerdict member name. |
| `factor_value_varies` | `bool` | — | never | Some factor value[...] column holds >= 2 distinct real answers. |
| `sample_is_described` | `bool` | — | never | Some characteristics[...] column other than organism and biological replicate holds a real answer. |
| `biological_replicate_varies` | `bool` | — | never | characteristics[biological replicate] holds >= 2 distinct real answers; false when the column is missing. |
| `row_count` | `int` | rows | never | Data rows in the document. |
| `column_names` | `string[]` | — | never | The evidence table's columns. |
| `columns` | `table` | — | never | One row per column a check read: factor values, then sample characteristics, then biological replicate, each in mzLib's (ordinal) order. |
| `caveats` | `string[]` | — | never | Four static caveats. |

Per-row fields (one value per record, in `columns` or each list entry):

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `source_index` | `int` | — | never | 0-based input position. *Present only with `--paths-stdin`.* |
| `source_path` | `string` | — | never | Absolute path. *Present only with `--paths-stdin`.* |
| `role` | `string` | — | never | factor_value \| sample_characteristic \| biological_replicate: which check read this column. |
| `column_name` | `string` | — | never | The column as the header spells it. |
| `rows` | `int` | rows | never | Rows, counted once per row per column NAME (a repeated column does not inflate it). |
| `filled` | `int` | rows | never | Rows with a real answer in at least one of the column's cells. |
| `absent` | `int` | rows | never | Rows that are empty or a reserved word (any case): rows - filled. |
| `distinct_values` | `int` | values | never | Different real answers, compared case-insensitively after trimming. |
| `fill_rate` | `float` | fraction (0-1) | never | filled / rows; 0 when rows is 0. May cross as an integer 0 or 1. |

### Errors

| Kind | pyMzLib raises | When |
|---|---|---|
| `usage` | [`UsageError`][pymzlib.UsageError] | as for sdrf validate (path / paths-stdin / threads / on-error rules, missing or repeated files) |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | mzLib cannot read a document |
| `service_unavailable` | [`ServiceUnavailableError`][pymzlib.ServiceUnavailableError] | never raised by this verb |

### Caveats

- Partial is often legitimate (a single-condition study has nothing to vary); the booleans say which check failed and the caller decides.
- On the 1,236-file bigbio curated corpus at 4f823dcd mzLib measured 300 Informative, 742 Partial, 194 Skeleton.
- Organism is excluded from sample_is_described because a search can fill it without a human describing the samples.
- Looks only at the sample half; says nothing about structure (sdrf validate) or cross-file agreement (sdrf lint).

### Example

```python
result = pymzlib.sdrf.assess('sdrf_cohort.sdrf.tsv')
```

Recorded from the live bridge as [`sdrf_assess_cohort.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/sdrf_assess_cohort.json). The doctests in the API reference replay these recordings in CI.

```python
batch = pymzlib.sdrf.assess_many(['sdrf_cohort.sdrf.tsv', 'sdrf_skeleton.sdrf.tsv', 'PXD000070.sdrf.tsv'])
```

Recorded from the live bridge as [`sdrf_assess_bulk.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/sdrf_assess_bulk.json). The doctests in the API reference replay these recordings in CI.

### Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.sdrf.assess` | `mzlib::sdrf::assess` with `BulkOptions` | `sdrf_assess` |

### References

- [doi:10.1038/s41467-021-26111-3](https://doi.org/10.1038/s41467-021-26111-3): SDRF-Proteomics sample columns (characteristics, factor value) (Dai et al., Nat Commun 12, 5854, 2021)

### Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | not yet shipped | not yet shipped |

!!! warning "Not yet verified"

    - fill_rate is a C# double that serialises 1.0 as 1; bindings must read it as a float.
    - since.pymzlib is null until released; Rust and R are not yet ported.
    - Bulk spellings are the cross-binding decision of 2026-09-23: a separate <verb>_many in every binding (R too, not vectorisation). Rust and R names here are intended, not yet ported.
