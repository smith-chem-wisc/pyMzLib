<!-- GENERATED by scripts/render_spec_docs.py from docs/specs/sdrf.parse-age.yaml (bridge 0388518f9f4f). Do not edit: fix the spec in the bridge, sync it, re-render. -->

> Read characteristics[age] cells into years with an honest precision, refusing any cell that would need a guess.

Wire verb `sdrf parse-age` · Python `pymzlib.sdrf.parse_ages` (not yet shipped)

### Wraps

- [`SdrfAge.TryParse`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/Sdrf/SdrfAge.cs) in `mzLib/Readers/Sdrf/SdrfAge.cs` at mzLib `23c2490e`

### Parameters

| Parameter | Type | Default | Unit | Range | Meaning |
|---|---|---|---|---|---|
| `stdin` | `string[]` | required | — | `>= 1 line` | One cell per line. BLANK LINES ARE KEPT (unlike every other stdin verb) so output row i is line i; one trailing newline does not add a cell. A UTF-8 BOM is dropped. |

### Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `cell_count` | `int` | cells | never | Cells read from stdin; the length of every column. |
| `parsed_count` | `int` | cells | never | Cells read as an age. |
| `column_names` | `string[]` | — | never | The table's columns. |
| `columns` | `table` | — | never | One row per input line, in order. |
| `caveats` | `string[]` | — | never | Four static caveats. |

Per-row fields (one value per record, in `columns` or each list entry):

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `cell` | `string` | — | never | The line exactly as given (mzLib's SdrfAge.Cell is the trimmed form). |
| `years` | `float` | years | yes: the cell was refused; refusal says why | The age, a range's MIDPOINT, or a bound. |
| `min_years` | `float` | years | yes: refused | Youngest the cell allows; 0 for UpperBound. |
| `max_years` | `float` | years | yes: refused, OR precision is LowerBound: UNBOUNDED (mzLib's +infinity, which JSON cannot carry) | Oldest the cell allows. |
| `precision` | `string` | — | yes: refused | Exact (incl. degenerate 40Y-40Y since #1333) \| Range \| LowerBound \| UpperBound. |
| `follows_specification` | `bool` | — | yes: refused | true for the spec grammar, false for unambiguous words ("3 year", "6-8 weeks", "4 hour"). |
| `refusal` | `string` | — | yes: the cell was read | empty \| reserved_word (any case) \| no_unit (a bare number) \| unreadable. |

### Errors

| Kind | pyMzLib raises | When |
|---|---|---|
| `usage` | [`UsageError`][pymzlib.UsageError] | stdin empty |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | never expected: TryParse does not throw |
| `service_unavailable` | [`ServiceUnavailableError`][pymzlib.ServiceUnavailableError] | never raised by this verb |

### Caveats

- Units are years: month 1/12, week 7/365.25, day 1/365.25, hour 1/(365.25 x 24).
- A bare number (63, 11% of real corpus age cells) is refused, never assumed to be years.
- years for a wide Range is a poor stand-in for any one sample; filter on precision.
- Every cell costs one line, not one process: send all cells in one call.

### Example

```python
ages = pymzlib.sdrf.parse_ages(['58Y', '30Y6M', '40Y-85Y', '40Y-40Y', '>=90Y', '<1Y', '6-8 weeks', '63', 'not available', '', 'about forty'])
```

Recorded from the live bridge as [`sdrf_parse_age.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/sdrf_parse_age.json). The doctests in the API reference replay these recordings in CI.

### Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.sdrf.parse_ages` | `mzlib::sdrf::parse_ages` | `sdrf_parse_ages` |

### References

- [doi:10.1038/s41467-021-26111-3](https://doi.org/10.1038/s41467-021-26111-3): SDRF-Proteomics age grammar nYnMnD (Dai et al., Nat Commun 12, 5854, 2021)

### Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | not yet shipped | not yet shipped | not yet shipped |

!!! warning "Not yet verified"

    - The refusal reason is a bridge-side classification of TryParse's false; see sdrf.samples.yaml. An upstream TryParse(cell, out age, out reason) would retire it.
    - Binding name is parse_ages (plural) for the verb parse-age (one cell per line). Kept because the function takes a list; revisit if a binding wants the wire name.
    - Rust and R spellings are intended, not yet ported. R: NA_character_ in the input should map to an empty line, as Python maps None.
    - since.pymzlib is null until released.
