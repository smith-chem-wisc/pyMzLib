<!-- GENERATED by scripts/render_spec_docs.py from docs/specs/version.yaml (bridge 48b007ffae00). Do not edit: fix the spec in the bridge, sync it, re-render. -->

> Report the bridge's version, its wire protocol, its .NET runtime, the mzLib it was built from, and every verb it dispatches.

Wire verb `version` · Python [`pymzlib.bridge_version`][pymzlib.bridge_version]

### Wraps

- [`AssemblyInformationalVersionAttribute`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/UsefulProteomicsDatabases/UsefulProteomicsDatabases.csproj) in `mzLib/UsefulProteomicsDatabases/UsefulProteomicsDatabases.csproj` at mzLib `23c2490e`

### Parameters

This verb takes no parameters.

### Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `bridge` | `string` | — | never | The bridge assembly's version. Not a compatibility number: use protocol. |
| `protocol` | `int` | — | never | The wire-format version; a binding is compatible with a bridge by this. |
| `runtime` | `string` | — | never | The bundled .NET runtime version. |
| `mzlib` | `string` | — | yes: the build recorded no mzLib commit | Which mzLib the bridge links, as 1.0.0+<commit>, read from the linked assembly. |
| `verbs` | `string[]` | — | never | Every verb this bridge dispatches, in dispatch order, GENERATED at build time from the DispatchAsync switch (BULK.md section 5). Absent from bridges before pyMzLib 0.2.0 (optional forever). A binding calling a verb its bridge does not list raises its usage error naming the release that has it, from that verb's since, instead of spawning a process. |

### Errors

| Kind | pyMzLib raises | When |
|---|---|---|
| `service_unavailable` | [`ServiceUnavailableError`][pymzlib.ServiceUnavailableError] | never raised by this verb |

### Caveats

- verbs lists what the bridge ROUTES, not which options each verb takes: a bulk option (--paths-stdin) on an older bridge that lists the verb still fails with that bridge's usage error.
- Bridges built before verbs existed omit the key; a binding must treat its absence as 'lists nothing', which correctly refuses every verb added since.

### Example

```python
info = pymzlib.bridge_version()
```

Recorded from the live bridge as [`bridge_version.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/bridge_version.json). The doctests in the API reference replay these recordings in CI.

### Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.bridge_version` | `mzlib::bridge_version` | `bridge_version` |

### References

None: no publication is attached to this verb.

### Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | 0.1.0 | 0.1.0 | 0.1.0 |

!!! warning "Not yet verified"

    - The Unknown-command message and verbs share one generated list (Program.Verbs, from the DeriveVerbList MSBuild task); check_verbs.py still parses the switch itself. Both read the same pattern, so they cannot disagree unless the switch stops being one arm per line.
    - pyMzLib returns version as a plain dict, typed as the TypedDict pymzlib.BridgeVersion so its keys are documented and linted; Rust and R spellings are the intended ones.
