<!-- GENERATED by scripts/render_spec_docs.py from docs/specs/readers.formats.yaml (bridge 48b007ffae00). Do not edit: fix the spec in the bridge, sync it, re-render. -->

> List every file type mzLib recognises, with the extension it dispatches on, the reader class that parses it, and the cross-format views it offers.

Wire verb `readers formats` · Python [`pymzlib.readers.formats`][pymzlib.readers.formats]

### Wraps

- [`SupportedFileType`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/Util/SupportedFileTypes.cs) in `mzLib/Readers/Util/SupportedFileTypes.cs` at mzLib `23c2490e`
- [`SupportedFileTypeExtensions.GetFileExtension`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/Util/SupportedFileTypes.cs) in `mzLib/Readers/Util/SupportedFileTypes.cs` at mzLib `23c2490e`
- [`SupportedFileTypeExtensions.GetResultFileType`](https://github.com/smith-chem-wisc/mzLib/blob/23c2490e/mzLib/Readers/Util/SupportedFileTypes.cs) in `mzLib/Readers/Util/SupportedFileTypes.cs` at mzLib `23c2490e`

### Parameters

This verb takes no parameters.

### Returns

Top-level fields:

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `format_count` | `int` | formats | never | Number of entries in formats; equal to the number of SupportedFileType members. 36 at mzLib 1.0.592 (32 at 1.0.591). |
| `formats` | `table` | — | never | One entry per SupportedFileType, in enum declaration order. Enumerated from mzLib, not transcribed, so it cannot drift from what mzLib dispatches. |

Per-row fields (one value per record, in `columns` or each list entry):

| Field | Type | Unit | Null? | Meaning |
|---|---|---|---|---|
| `file_type` | `string` | — | never | mzLib's SupportedFileType member name, e.g. "MsFraggerPsm", "psmtsv", "MzIdentMLGz". |
| `extension` | `string` | — | yes: mzLib has no extension mapping for this member (a broken mzLib build); the listing reports it rather than failing | The suffix mzLib dispatches on, e.g. ".mzid.gz" or "QuantifiedProteinGroups.tsv". NOT unique: both Bruker types are ".d" and several share ".tsv"; DiaNnReport and PytheasResult dispatch on content, not on this value. |
| `reader` | `string` | — | yes: mzLib has no reader mapping for this member (a broken mzLib build) | The mzLib reader class name, e.g. "MzIdentMLResultFile". |
| `views` | `string[]` | — | never | Cross-format views this type offers, from quantifiable, ms1_features, spectra, spectral_match. Empty is a real answer: 17 of 36 types offer none and are readable only through read-records. |

### Errors

| Kind | pyMzLib raises | When |
|---|---|---|
| `usage` | [`UsageError`][pymzlib.UsageError] | never raised by this verb |
| `correctness` | [`BridgeError`][pymzlib.BridgeError] | never raised by this verb |
| `service_unavailable` | [`ServiceUnavailableError`][pymzlib.ServiceUnavailableError] | never raised by this verb |

### Caveats

- Extensions are not unique and not always how dispatch works: identify a real path rather than matching on extension.
- An empty views list means mzLib parses the format into its own record type with no cross-format interface; read-records still reads it.
- At mzLib 1.0.592 the view families are: quantifiable 4, ms1_features 2, spectral_match 6, spectra 7, none 17. These counts are the pin's, not the contract's: bindings should call formats rather than repeat them in prose.

### Example

```python
formats = pymzlib.readers.formats()
```

Recorded from the live bridge as [`readers_formats.json`](https://github.com/smith-chem-wisc/pyMzLib/blob/main/pkg/python/tests/fixtures/readers_formats.json). The doctests in the API reference replay these recordings in CI.

### Same verb in other bindings

| Python (pyMzLib) | Rust (mzLibRust) | R (mzLibR) |
|---|---|---|
| `pymzlib.readers.formats` | `mzlib::readers::formats` | `readers_formats` |

### References

None: no publication is attached to this verb.

### Since

| Wire protocol | pyMzLib | mzLibRust | mzLibR |
|---|---|---|---|
| 1 | 0.1.0 | 0.1.0 | 0.1.0 |
